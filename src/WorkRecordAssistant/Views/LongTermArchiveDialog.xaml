<Window x:Class="WorkRecordAssistant.Views.LongTermArchiveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:WorkRecordAssistant.Converters"
        xmlns:models="clr-namespace:WorkRecordAssistant.Models"
        Title="任务归档箱"
        Width="380" Height="460"
        MinWidth="320" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Segoe UI Variable, Segoe UI, Microsoft YaHei UI">

    <Window.Resources>
        <conv:CountToVisibilityConverter x:Key="CountVis" />
        <conv:CountToVisibilityConverter x:Key="EmptyVis" />
        <conv:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVis" />
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="已完成任务"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,8" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Style="{StaticResource SecondaryButton}"
                    Content="全部"
                    Padding="12,4"
                    Margin="0,0,6,0"
                    Command="{Binding SetFilterCommand}"
                    CommandParameter="{x:Static models:ArchiveTaskFilter.All}" />
            <Button Style="{StaticResource SecondaryButton}"
                    Content="星级"
                    Padding="12,4"
                    Margin="0,0,6,0"
                    Command="{Binding SetFilterCommand}"
                    CommandParameter="{x:Static models:ArchiveTaskFilter.Starred}" />
            <Button Style="{StaticResource SecondaryButton}"
                    Content="普通"
                    Padding="12,4"
                    Command="{Binding SetFilterCommand}"
                    CommandParameter="{x:Static models:ArchiveTaskFilter.Normal}" />
        </StackPanel>

        <Border Grid.Row="2"
                Style="{StaticResource CardBorder}"
                Padding="4">
            <Grid>
                <TextBlock Text="暂无归档任务"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding Items.Count, Converter={StaticResource EmptyVis}, ConverterParameter=Inverse}" />

                <ListBox ItemsSource="{Binding Items}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Visibility="{Binding Items.Count, Converter={StaticResource CountVis}}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="6"
                                    Padding="8,6"
                                    Margin="0,2">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsStarred}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource StarredTaskBackgroundBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding DisplayText}"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontSize="13"
                                               VerticalAlignment="Center" />
                                    <Button Grid.Column="1"
                                            Style="{StaticResource SecondaryButton}"
                                            Content="复制"
                                            Padding="10,4"
                                            Margin="8,0,0,0"
                                            VerticalAlignment="Center"
                                            Command="{Binding DataContext.CopyItemCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="11"
                   Margin="0,8,0,0"
                   HorizontalAlignment="Center"
                   Visibility="{Binding StatusMessage, Converter={StaticResource StringNotEmptyToVis}}" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Style="{StaticResource CopyButton}"
                    Content="复制全部"
                    Width="96"
                    Margin="0,0,8,0"
                    Command="{Binding CopyAllCommand}" />
            <Button Style="{StaticResource SecondaryButton}"
                    Content="关闭"
                    Width="80"
                    Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>
